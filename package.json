{
    "name": "rover-router",
    "version": "1.0.0",
    "description": "Cloudflare worker prototype",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Adam Pacholski <adam.pacholski@rover.com>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
